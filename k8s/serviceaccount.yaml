apiVersion: v1
kind: ServiceAccount
metadata:
  name: vulncopilot
  namespace: rag
# The pod needs no AWS data-plane access (Postgres is reached via PG_DATABASE_URL,
# not IAM), so the projected SA token would only be attack surface if mounted.
automountServiceAccountToken: false
