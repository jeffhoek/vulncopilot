"""Per-metric hard floors for the offline eval suite.

Generated by `evals/derive_thresholds.py` from baseline runs committed
under `evals/baselines/`. Until ≥3 baselines land, the values below are
the strawman starting points from `plans/eval-framework.md` — they will
fail several entries on a typical run, which is expected and exercises
the assertion wiring.
"""

from __future__ import annotations

THRESHOLDS: dict[str, float] = {
    "faithfulness": 0.80,
    "context_recall": 0.70,
    "answer_correctness": 0.60,
}
